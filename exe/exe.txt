t : print a short string -- just a temporary debug aid
q : quit
+ : push(pop + pop)
- : push(pop - pop)
* : push(pop * pop)
/ : push(pop / pop)
% : push(pop % pop)
. : push(catenate(pop,pop))
= : print(pop)
? : print(top)
dec : decimal base for output
hex : hex base for output
oct : octal base for output
not : not(0) -> 1; not(n) -> 0, when n != 0
ne : ! ( pop eq pop )
eq : ( pop eq pop )
lt : ( pop lt pop )
gt : ( pop gt pop )
le : ( pop le pop )
ge : ( pop ge pop )
cmp :
depth : number of entries on the stack
dup : push(top)  a b c -> a b c c
drop : a b c -> a b
over : a  b c -> a b c b
rot : a b c -> b c a
exch : a b -> b a
clear : clears stack
.c : clears stack
exec : execute(pop)
.x : execute(pop)
rpt : "{ code } n rpt" executes the "code" n times.  example: 
        1 { 1 + } 8 rpt -> 9
def : "1 <x> def" creates a variable x and gives it a value of 1.  if the
        variable exists, it is updated.  
set : "1 set x" creates a variable x
!
@
loop
if
load
.s
namelist
codelist
mem
mem_trace
dmptop
dmpvar
push
verbose : show flags on stack dump
.v : verbose
 _UNDEFINED_ 
 _BRK_ 
break
 _EXIT_ 
exit
[ : 
] :
{ : begin a compiled block
} : end compiled block
count_to_mark
$
< : begin a string
> : end of a string


Examples:

>{ 1 + } set a  # define a thread object, "a"
>1 a .s         # it executes immediately
  2
>a a a a .s
  6
>a 3 rpt .s     # "a" leaves 7 on stack, which gets repeated 3 times
  7
  7
  7
>{ a 3 rpt } set b  # in thread context, however, the word is
>.c                 # not executed immediately
>1 b .s
  4
>{ a 4 rpt } .s     # a thread object just sitting on the stack
  [T]               # can be executed by ".x"
  4
>.x .s
  8
>

