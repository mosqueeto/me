bugs: 
pipe buffer through command clears buffer
    message line shows "pipe: ld bytes" instead of anything useful
macro example: bind M-r pandoc -f markdown -t plain @render doesn't produce 
a buffer named render that we can do anything with (not in list-buffers) 
however, if I try to use buffer "render" (M-b) it says it is a reserved name.

can we fix the 8-bit ascii display? do I want to? can we instead add a
command to change some bytes with high bit set to nearest 7-bit ascii
equivalent? (internal table of equivalents. there are probably fewer than 10 
cases that matter)

